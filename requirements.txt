fastapi==0.141.1
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
python-dotenv>=1.0
groq>=1.7
numpy>=2.0
praat-parselmouth>=0.4.7

# Postgres, used when DATABASE_URL is set. Harmless locally, where the app
# falls back to SQLite.
psycopg[binary]>=3.2
psycopg_pool>=3.2

# Google sign-in
authlib>=1.3
httpx>=0.27
itsdangerous>=2.1
